// operators available in JS:

// finding discount on an product:

var sellPrice = 800;
var originalPrice = 1350;

var discountPrice = ((originalPrice - sellPrice)/originalPrice)*100;

console.log(`
    Discount on the product is: ${discountPrice}
`);